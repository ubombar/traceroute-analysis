# Traceroute Analysis

This repository is setup to save the scripts that are used for the analysis of Ark, Iris, and Retina platforms.

## Usage 

For invoking the functions simply run the python scripts under the `scripts` directory. Please use the `make` command to interact with the environment.

